type Produto {
  id: ID!
  nome: String!
  descricao: String
  preco: Float!
  categoria: Categoria
}

extend type Query {
  produtos: [Produto]
  produto(id: ID!): Produto
}

extend type Mutation {
  criarProduto(
    nome: String!
    descricao: String
    preco: Float!
    categoria_id: ID
  ): Produto @auth(roles: ["RESTAURANTE"])

  editarProduto(
    id: ID!
    nome: String!
    descricao: String
    preco: Float!
    categoria_id: ID
  ): Produto @auth(roles: ["RESTAURANTE"])

  deletarProduto(id: ID!): Boolean @auth(roles: ["RESTAURANTE"])
}
